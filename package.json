{
  "name": "keys-to-object",
  "version": "0.2.0",
  "description": "Map array of keys into a new object",
  "author": "Jon Whitehead",
  "license": "MIT",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "scripts": {
    "prettier": "prettier --single-quote --trailing-comma all --write \"src/**/*.ts\"",
    "clean": "rimraf lib/*",
    "build": "yarn run clean && tsc",
    "prepare": "yarn run build"
  },
  "devDependencies": {
    "prettier": "^1.9.2",
    "rimraf": "^2.6.2",
    "typescript": "^2.6.2"
  }
}
