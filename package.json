{
  "name": "keys-to-object",
  "version": "0.2.0",
  "description": "Map an array of keys into a new object",
  "homepage": "https://github.com/jsiwhitehead/keys-to-object",
  "license": "MIT",
  "author": "Jon Whitehead",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "prettier": {
    "singleQuote": true,
    "trailingComma": "all"
  },
  "scripts": {
    "prettier": "prettier --write \"src/**/*.ts\"",
    "clean": "rimraf lib/*",
    "build": "yarn run clean && tsc",
    "prepare": "yarn run build"
  },
  "devDependencies": {
    "prettier": "^1.13.7",
    "rimraf": "^2.6.2",
    "typescript": "^2.9.2"
  }
}
